const fetchNews = async (
    category?: Category | string,
    keywords?: string,
    isDynamic?: boolean
) => {
    //Graph ql query

    //fetch function with next 13 caching....

    //sort article by images

    //return result
}

export default fetchNews